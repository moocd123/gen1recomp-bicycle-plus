# Scheduled development checkpoint

Verified implementation commit: `7bead8aa264d07f00a736adc1817fa75bb6f12bd` (`Wire isolated local songs into FireRed audio and menus`).

This milestone adds a beta-only, content-addressed local-audio library for MP3/OGG/WAV/FLAC payloads, isolated under `autobike_plus_firered_beta` storage; SHA-256 integrity checks; decoder validation; local-song browsing/selection in the FireRed-native BIKE SONG menu; and a third independent cycling-audio backend for validated local streaming Sources. Local playback shares the existing cycling volume/filter, AREA/BICYCLE/BOTH, fanfare suspension and restart/resume behaviour without taking ownership of the native FireRed BGM worker. Existing FireRed M4A and imported Gen1/2 ChipSynth paths remain intact. The stable Gen1/2 runtime, manifest, library and publishing workflow remain unchanged.

Verification: Gen 3 lab Actions run `35503358044` completed successfully on commit `7bead8aa264d07f00a736adc1817fa75bb6f12bd`. The immediately preceding run `35503238473` also passed the new isolated library after a test-only lifecycle assertion fix at `f962086dc338f4e92417073b5355d9a94aaa3a22`. Stable v1.9.3 regression checks passed for all six Gen1/2 editions. FireRed mount, native menus, current/legacy/local song catalogues and selectors, shade transform, isolated settings, independent M4A/ChipSynth/local cycling audio, lifecycle integration, player paint/preview, colour picker, pointer bridge and beta manifest gate all passed. The local library test exercises content IDs, namespace confinement, duplicate handling, rename/remove, corruption rejection, safe mode and decoder/source lifecycle with a software decoder double. These are not ROM, physical-device, native-dialog or listening tests.

Remaining milestones before an installable candidate: connect the isolated local library to the platform-aware Import Song picker (mobile native document picker, desktop FilePicker and in-app browser fallback), including pending-result polling and user-visible success/error handling; retain local rename/remove controls in the Gen3 UI; stage the FireRed-only beta directory and produce/validate a read-only Actions ZIP artifact with manifest/checksums; then perform ROM-dependent in-motion mask review and physical-device/native-picker/listening checks in the interactive session because scheduled runs cannot access the user's uploaded ROMs or hardware.
